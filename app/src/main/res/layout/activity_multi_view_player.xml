<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Lưới các video đang xem cùng lúc (2/3/4 ô, chia đều) -->
    <LinearLayout
        android:id="@+id/grid_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" />

    <!-- Video đang được phóng to ở giữa màn hình (video khác vẫn tiếp tục chạy phía sau lưới nhỏ) -->
    <FrameLayout
        android:id="@+id/enlarged_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/btn_collapse_enlarged"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:background="#88000000"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:contentDescription="@string/btn_back_to_grid"
        android:visibility="gone" />

</FrameLayout>
